<examples:ExamplePage xmlns="http://www.nativescript.org/tns.xsd" xmlns:examples="examples/example-base-page" navigatingTo="pageNavigatingTo" backgroundSpanUnderStatusBar="true">
    <Page.actionBar>
		<ActionBar title="All Sessions" />
    </Page.actionBar>
    <GridLayout rows="auto, auto, *" columns="*" cssClass="main-content">
        <!-- Segmented Bar -->
        <SegmentedBar selectedIndex="{{ selectedDay }}"
            ios:selectedBackgroundColor="#FBE5AE"
            android:selectedBackgroundColor="#FAC950" 
            cssClass="conf-segmented-bar">
            <SegmentedBar.items>
                <SegmentedBarItem title="MAY 3" />
                <SegmentedBarItem title="MAY 4" />
                <SegmentedBarItem title="MAY 5" />
            </SegmentedBar.items>
        </SegmentedBar>
        <!-- Search -->
        <SearchBar row="1" text="{{ search }}" hint="Search" id="search" />
        <!-- List -->
        <ListView row="2" items="{{ sessions }}" id="sessions-list">
            <ListView.itemTemplate>
                <GridLayout columns="*, auto" rows="auto, auto, auto" cssClass="session-container">
					<Image row="1" col="1" src="{{ isFavourite ? 'res://add_to_fav' : 'res://add_to_fav_1' }}" tap="toggleFavourite" />
                    <Label row="0" text="{{ range }}" cssClass="session-time" />
                    <Label row="1" text="{{ title }}" textWrap="true" cssClass="session-title" />
                    <Label row="2" text="{{ room }}" cssClass="session-room" />                    
                </GridLayout>
            </ListView.itemTemplate>
        </ListView>
    </GridLayout>
</examples:ExamplePage>
