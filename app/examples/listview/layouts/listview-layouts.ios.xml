<examples:ExamplePage  
        xmlns="http://www.nativescript.org/tns.xsd" 
        xmlns:lv="nativescript-telerik-ui-pro/listview"
        xmlns:examples="examples/example-base-page"
        navigatingTo="pageNavigatingTo">
    <Page.actionBar>
        <ActionBar title="Recipes" automationText="ActionBar">
            <NavigationButton icon="res://ic_back" tap="goBack" automationText="GoBack" />
            <ActionBar.actionItems>
                <ActionItem icon="{{isLinearActive ? 'res://listview/layouts/icons/list-layout' : 'res://listview/layouts/icons/list-layout-1' }}" tap="onLinearLayoutTap" ios.position="right"/>
                <ActionItem icon="{{isWrapActive ? 'res://listview/layouts/icons/wrap-layout' : 'res://listview/layouts/icons/wrap-layout-1' }}" tap="onGridLayoutTap" ios.position="right"/>
                <ActionItem id="exampleMenuButton" icon="res://ic_menu_example" ios.position="right" automationText="ExampleMenu" />
            </ActionBar.actionItems>
        </ActionBar>
    </Page.actionBar>
	<GridLayout rows="*" >
        <lv:RadListView id="list-view" row="0" items="{{ pictures }}" marginRight="-2">
            <lv:RadListView.listViewLayout>
                <lv:ListViewLinearLayout ios.dynamicItemSize="false" itemHeight="250" scrollDirection="Vertical"/>
            </lv:RadListView.listViewLayout>
            <lv:RadListView.itemTemplate>
                <GridLayout rows="*" backgroundColor="White">
                   <GridLayout rows="*" marginBottom="2" marginRight="2">
                        <Image src="{{ photo ? 'res://listview/layouts/' + photo + '.jpg' : 'res://'}}" stretch="aspectFill"/>
                        <GridLayout verticalAlignment="bottom">
                            <StackLayout backgroundColor="#BBFFFFFF" paddingTop="8" paddingBottom="8" paddingLeft="16" paddingRight="16">
                                <Label text="{{ title }}" cssClass="recipeTitle" textWrap="true"/>
                                <Label text="{{ author }}" cssClass="recipeAuthor"/>
                            </StackLayout>
                        </GridLayout>
                   </GridLayout>
                </GridLayout>
           </lv:RadListView.itemTemplate>
        </lv:RadListView>
    </GridLayout>
</examples:ExamplePage>