<Page xmlns="http://www.nativescript.org/tns.xsd"
    loaded="pageLoaded"
    navigatedTo="pageNavigatedTo"
    cssClass="detailPageStyle">

    <!-- <Page.actionBar >
        <ActionBar  title="" style="actionBarStyle">
            <NavigationButton  tap="navigateBack" />
            <ActionItem icon="res://add_to_fav_icon"  text="aaa" position="right" tap="addToFav"  />
        </ActionBar>
    </Page.actionBar> -->

    <StackLayout rows="auto, *" orientation="vertical" >
      <Label text="{{ Title }}" cssClass="detailTitle" textWrap="true" />
      <Label text="{{ Content }}" cssClass="detailContent" textWrap="true"/>
    </StackLayout>

</Page>
