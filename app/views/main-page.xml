<Page xmlns="http://www.nativescript.org/tns.xsd" xmlns:drawer="nativescript-telerik-ui/sidedrawer" loaded="pageLoaded">
  <Page.actionBar>
    <ActionBar title="NativeScript Demos" />
  </Page.actionBar>
  <drawer:SideDrawer drawerLocation="Bottom">
    <drawer:SideDrawer.drawerContent>
      <StackLayout cssClass="drawerContent">
        <Label cssClass="drawerItem" text="About" />
        <Label cssClass="drawerItem" text="Product Page" />
        <Label cssClass="drawerItem" text="Documentation" />
        <Label cssClass="drawerItem" text="SDK examples" />
        <Label cssClass="drawerItem" text="Send Feedback" />
      </StackLayout>
    </drawer:SideDrawer.drawerContent>
    <drawer:SideDrawer.mainContent>
      <GridLayout rows="auto, auto, *" columns="*">
        <!-- Segmented bar navigation -->
        <SegmentedBar selectedIndex="{{ selectedScreen }}">
          <SegmentedBar.items>
            <SegmentedBarItem title="Featured examples" />
            <SegmentedBarItem title="All controls" />
          </SegmentedBar.items>
        </SegmentedBar>
        <!-- Show New filter -->
        <StackLayout orientation="horizontal" horizontalAlignment="right" row="1">
          <Label text="Show only new" />
          <Switch checked="{{ showOnlyNew }}" />
        </StackLayout>
        <!-- Featured examples -->
        <GridLayout visibility="{{ selectedScreen===0 ? 'visible' : 'collapsed' }}" row="2" columns="*" rows="*">
          <ScrollView>
            <Repeater items="{{ featuredExamples }}">
              <Repeater.itemTemplate>
                <GridLayout rows="auto, auto, *" columns="auto, *" tap="navigateToExample" margin="10">
                  <Image rowSpan="3" src="{{ image }}" cssClass="example-image-big"  verticalAlignment="top" horizontalAlignment="left"/>
                  <Label col="1" horizontalAlignment="left" text="{{ title }}" cssClass="example-title" />
                  <Label col="1" horizontalAlignment="right" text="NEW" cssClass="new" visibility="{{ isNew ? 'visible' : 'collapsed' }}" />
                  <TextView col="1" row="1" text="{{ info }}" editable="false" cssClass="info" />
                  <Repeater col="1" row="2" verticalAlignment="bottom" items="{{ controls }}">
                    <Repeater.itemsLayout>
                      <WrapLayout />
                    </Repeater.itemsLayout>
                    <Repeater.itemTemplate>
                        <Label text="{{ $value }}" cssClass="control" />
                    </Repeater.itemTemplate>
                  </Repeater>
                </GridLayout>
              </Repeater.itemTemplate>
            </Repeater>
          </ScrollView>
        </GridLayout>
        <!-- All examples -->
        <GridLayout visibility="{{ selectedScreen===1 ? 'visible' : 'collapsed' }}" row="2" columns="*" rows="*">
          <Repeater items="{{ exampleGroups }}">
            <Repeater.itemTemplate>
              <StackLayout tap="navigateToExampleGroup" orientation="horizontal" margin="10">
                <Label text="{{ title }}" cssClass="example-title" />
                <Label text="NEW" cssClass="new" visibility="{{ isNew ? 'visible' : 'collapsed' }}" />
              </StackLayout>
            </Repeater.itemTemplate>
          </Repeater>
        </GridLayout>
      </GridLayout>
    </drawer:SideDrawer.mainContent>
  </drawer:SideDrawer>
</Page>