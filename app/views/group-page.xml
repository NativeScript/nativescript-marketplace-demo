<Page xmlns="http://www.nativescript.org/tns.xsd" navigatedTo="pageNavigatedTo">
  <Page.actionBar>
    <ActionBar title="{{ group.title }}">
      <NavigationButton tap="navigateBack" />
    </ActionBar>
  </Page.actionBar>
  <GridLayout rows="auto, *" columns="*">
    <!-- Show New filter -->
    <StackLayout orientation="horizontal" horizontalAlignment="right" margin="4 0 0 0">
      <Label text="Show only new" verticalAlignment="center" />
      <Switch checked="{{ showOnlyNew }}" verticalAlignment="center" />
    </StackLayout>
    <!-- Examples list -->
    <GridLayout row="1" columns="*" rows="*">
      <ScrollView>
        <Repeater items="{{ examples }}">
          <Repeater.itemTemplate>
            <GridLayout rows="auto, auto, *" columns="auto, *" tap="navigateToExample" cssClass="example-item" 
               visibility="{{ $parents['Repeater'].showOnlyNew, (!$parents['Repeater'].showOnlyNew || isNew) ? 'visible' : 'collapsed' }}">
              <Image rowSpan="3" src="{{ image }}" cssClass="example-image-big" cssClass="list-example-image" />
              <Label col="1" horizontalAlignment="left" text="{{ title }}" cssClass="example-title" />
              <Label col="1" horizontalAlignment="right" text="NEW" cssClass="new" visibility="{{ isNew ? 'visible' : 'collapsed' }}" />
              <TextView col="1" row="1" text="{{ info }}" editable="false" cssClass="info" tap="navigateToExample"/>
              <Repeater col="1" row="2" verticalAlignment="bottom" items="{{ controls }}">
                <Repeater.itemsLayout>
                  <WrapLayout />
                </Repeater.itemsLayout>
                <Repeater.itemTemplate>
                  <Label text="{{ $value }}" cssClass="control" tap="controlTap" />
                </Repeater.itemTemplate>
              </Repeater>
            </GridLayout>
          </Repeater.itemTemplate>
        </Repeater>
      </ScrollView>
    </GridLayout>
  </GridLayout>
</Page>